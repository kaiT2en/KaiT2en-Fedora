PACKAGE_NAME="i915"
PACKAGE_VERSION="0.1"

MAKE[0]="make -C /lib/modules/${kernelver}/build M=${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/build KBUILD_EXTRA_SYMBOLS=/var/lib/dkms/t2gmux/0.1/${kernelver}/${arch}/module/Module.symvers"
CLEAN="make -C /lib/modules/${kernelver}/build M=${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/build clean"

BUILT_MODULE_NAME[0]="i915"
DEST_MODULE_LOCATION[0]="/updates/dkms"

AUTOINSTALL="yes"
